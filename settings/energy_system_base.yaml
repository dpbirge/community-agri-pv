# Shared Energy System Configuration
# Defines the energy system shared throughout the community
# Defines ALL wind systems as they do not interact with farming operations
# Defined any non-agri-pv based solar panels, as all agri-pv systems are defined
# at the farm level. Panel location is not specified intentionally.
#
# Data files: see energy_supply section in settings/data_registry.yaml

config_name: baseline_energy_system

# --- Solar System ---
community_solar:
  low_density:
    area_ha: 0.0
  medium_density:
    area_ha: 0.0
  high_density:
    area_ha: 0.05
 
# --- Wind System ---
wind_turbines:
  small_turbine:
    number: 5
  medium_turbine:
    number: 0
  large_turbine:
    number: 0

# --- Generator System ---
generator:
  has_generator: true
  type: diesel_100kw          # row key in generators-toy.csv (capacity_kw=100)
  rated_capacity_kw: 50

# --- Battery System ---
battery:
  has_battery: true
  type: lithium_iron_phosphate_medium   # row key in batteries-toy.csv (200 kWh LFP)
  capacity_kwh: 1000

# --- Grid Connection ---
grid_connection: full_grid